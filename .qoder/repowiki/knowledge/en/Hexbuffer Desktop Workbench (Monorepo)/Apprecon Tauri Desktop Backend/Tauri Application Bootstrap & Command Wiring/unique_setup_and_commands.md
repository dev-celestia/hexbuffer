Sets `TOKIO_WORK Worker_THREADS=32` before Tauri init to prevent tauri-plugin-pty starvation. Each launch truncates `/tmp/hexbuffer.log`. A 10-second async fallback closes the splash screen if the React frontend fails to call `show_main_window`. The `open_cdp_browser` command auto-discovers Chrome/Chromium candidates and kills existing CDP instances on Unix via `lsof` + `kill -9`.