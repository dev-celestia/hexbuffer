No build or setup script is specific to this module; it is consumed as part of the larger React/Tauri application. The settings page relies on Tauri backend commands being registered (e.g. `get_ai_settings`, `save_ai_settings`, `get_r2_settings`, `save_r2_credentials`, `reset_local_data`, `reset_database`, `reset_all_app_data`, `get_storage_info`, `get_ca_cert`, `regenerate_ca_cert`, `trust_intercept_ca`).