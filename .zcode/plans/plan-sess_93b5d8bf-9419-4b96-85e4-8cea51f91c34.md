# Update Hexbuffer website docs to match current release features (v1.1.1)

Goal: make `docs/website` document **only the release feature set** from `src/layout/constants.ts` (Desktop, HTTP, Intercept, API Mock, API Override, Notes, Intruder, Repeater, Port Scanner, JWT, Settings), with naming synced to the current app and new pages for API Mock / API Override.

## 1. Landing page — `content/docs/index.mdx`
- Bump **v1.1.0 → v1.1.1**; rewrite tagline paragraph to drop "visual workflow automation" and "AI guidance" claims.
- Feature cards: replace "Autonomous AI Copilot" and "Visual Workflow Automation" cards with cards for **API Mock & Override** and **Request Crafting & Fuzzing (Intruder)**; fix card links to new section paths.
- Rewrite "Core Capabilities at a Glance": Intruder (not Invoker, Sniper-only framing), remove SQL Injection/XSS, OOB Listener, AI copilot, Kanban, PTY Terminal, Encoder/Hasher mentions; add API Mock (standalone mock server) and API Override (Map Local rules); remove "(Beta)" from Port Scanner; mark API Mock, API Override, Port Scanner, JWT as **New** (matching `isNew` in constants).
- Rewrite "Explore the Documentation" section links to the new structure.

## 2. Delete alpha/removed-feature pages
- Delete `ai-and-automation/` entirely (AI Assistant, workflows, regression, browser) and remove its `meta.json`.
- Delete `traffic-and-inspection/websocket-history.mdx` (WebSocket is alpha).
- Delete `request-crafting-and-testing/listener.mdx` and `security-utilities.mdx` (Listener alpha; SQLi/XSS generator removed from app).
- Delete `developer-workspace/` pages: `index.mdx` (Mock Forge → replaced by API Mock/Override pages), `flow-components.mdx`, `data-tools.mdx` (Encoder/Hash alpha; JWT gets its own page), `notes-and-tasks.mdx` (replaced by a notes page without Kanban).

## 3. Restructure & update remaining sections

**`traffic-and-inspection/`** — meta: index, intercept, inspector.
- `index.mdx` (Live HTTP History): add session management (ephemeral/persistent storage modes, per-session counts), chunked pagination behavior + dismissible alert, request body preview, expanded modal detail view, context-menu actions (Send to Intruder / Intercept / API Override / Notes, copy as curl, pinning, groups, highlights, blacklisting).
- `intercept.mdx`: refresh with multi-tab capture filters and queue/request editor workflow.
- `inspector.mdx`: retitle to **"Deep Message Inspector"**; drop all Monaco Comparer content (Comparer is alpha).

**`request-crafting-and-testing/`** — meta: index (Repeater), intruder, port-scanner.
- `invoker.mdx` → rename to `intruder.mdx`; rewrite: **Intruder** naming, Sniper attack mode, `§...§` payload positions with auto-detection, payload types (SimpleList / RuntimeFile / NumberRange) + processing rules (URL/Base64/hash), bundled wordlist presets, engine controls (concurrency/delay/retries/redirects), Grep Match/Extract, session handling rules, results filtering and split/full-width inspector.
- `index.mdx` (Repeater): update for workspaces, collection tree with drag-and-drop/import, environment contexts, pre/post-request scripting, request deletion, custom method/headers/body.
- `port-scanner.mdx`: remove all "(Beta)" badges; document presets (Quick/Web/Top 100/Full/Custom), custom ports dialog, concurrency/timeout, stealth vs high-performance mode, banner grabbing.

**New section `api-tools/`** ("API Tools & Security", inserted after request-crafting):
- `api-mock.mdx` (new, **New** badge): standalone localhost mock server (default port 4000), domains/routes with SSL flag, methods/status/headers, `:param`/`{param}` dynamic routes, `{{param.id}}`/`{{query.x}}` templating with live preview, request matchers (header/query/body), chaos engineering (latency + error rate), CORS preflight, per-route request logs (200-entry buffer).
- `api-override.mdx` (new, **New** badge): Map Local-style proxy response overrides, same rule engine, host added via right-click → "Send to API Override" from HTTP History, templated responses, chaos on proxied traffic, override logging, enable/disable per domain/route.
- `jwt.mdx` (moved content from data-tools, **New** badge instead of Beta): Decode/Generate tabs, vulnerability scanning with severity cards, HS/RS/ES/PS algorithms + `none`, secret/PEM signing, key generation.

**New section `workspace/`** ("Notes & Workspace"):
- `notes.mdx` (new, based on notes-and-tasks content minus Kanban/terminal/file manager): multi-tab scratchpad, Editor/Code view modes, drawing canvas (tools, templates, PNG insertion into notes), Saved Notes manager (search/filter/sort/duplicate/export/rename/delete), tab management, Desktop Scratchpad widget sharing.

**`getting-started/index.mdx`**: keep proxy/CA-cert/first-capture flow; trim widget list and any references to removed tools (terminal, file manager, AI).

**`user-guide-and-faqs/index.mdx`**: remove FAQ/tips for AI (Ollama), peer discovery, Comparer, and other removed tools; verify keyboard shortcuts against current app config and correct stale ones (e.g. Cmd+I Invoker); keep export/performance tips that still apply. Update "Explore the Documentation" style links if present.

**`content/docs/meta.json`** (root nav): Core Features separator → traffic-and-inspection, request-crafting-and-testing, api-tools, workspace, then User Guide separator → user-guide-and-faqs.

## 4. Verification
- Run `pnpm build` (or `pnpm dev`) inside `docs/website` to confirm fumadocs builds with no broken nav/meta references and no links to deleted pages.
- Grep the content tree for leftover terms: `Invoker`, `Mock Forge`, `Beta`, `WebSocket`, `Kanban`, `Listener`, `AI`, `Comparer`, `XSS`, `v1.1.0` — clean any stragglers.
- Note: `lib/shared.ts` (appName "Hexbuffer", GitHub repo config) is left as-is; `out/` build output is not committed.

Per AGENTS.md, this is docs-only work (no React component changes), so the Tailwind/component rules don't apply here.